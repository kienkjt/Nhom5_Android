<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyHealth"
        tools:targetApi="31">

        <!-- WeightTrackerActivity là màn hình chính -->
        <activity android:name=".activities.WeightTrackerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        &lt;!&ndash; MainActivity vẫn giữ lại để mở khi cần &ndash;&gt;-->
<!--        <activity android:name=".activities.MainActivity" />-->

<!--        &lt;!&ndash; Activity khác &ndash;&gt;-->
<!--        <activity android:name=".activities.AddRecordActivity" />-->

<!--        &lt;!&ndash; Receiver &ndash;&gt;-->
<!--        <receiver android:name=".services.ReminderService" />-->

    </application>

</manifest>
